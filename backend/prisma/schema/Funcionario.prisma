enum Permissao {
    ADMINISTRADOR
    OPERADOR 
    ENGENHEIRO
}

model Funcionario {
    id Int @id @default(autoincrement())
    nome String
    telefone String @unique
    endereco String 
    usuario  String @unique 
    senha String 
    etapas FuncEtapa[]
    nivelPermissao Permissao
}